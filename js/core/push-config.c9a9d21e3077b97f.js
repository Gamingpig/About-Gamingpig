/* Public configuration only. Replace after rotating VAPID and generating RSA keys. */
window.PushConfig = Object.freeze({
    vapidPublicKey: '',
    registrationPublicKey: null,
    queueUrl: 'https://ntfy.sh/gamingpig_push_sub_vault_v2_9f8a2b3c4d5e'
});
